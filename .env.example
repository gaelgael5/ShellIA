# ShellIA - Configuration Environment Variables

# ============================================================================
# AI PROVIDER SELECTION
# ============================================================================
# Choisir le provider IA : "chatgpt" ou "claude"
AI_PROVIDER=claude

# ============================================================================
# CLAUDE (ANTHROPIC) CONFIGURATION
# ============================================================================
# Obtenir votre clé API : https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-votre-clé-ici

# Modèle Claude à utiliser (optionnel, défaut: claude-sonnet-4-20250514)
# Options:
#   - claude-opus-4-20250514   (le plus puissant, plus cher)
#   - claude-sonnet-4-20250514 (équilibre performance/coût) ✅ RECOMMANDÉ
#   - claude-haiku-4-20250514  (rapide, moins cher)
CLAUDE_MODEL=claude-sonnet-4-20250514

# ============================================================================
# CHATGPT (OPENAI) CONFIGURATION
# ============================================================================
# Obtenir votre clé API : https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-votre-clé-openai-ici

# Note: Le modèle ChatGPT (gpt-4o-mini) est défini dans le code
# Pour changer le modèle, modifiez src/core/ai_chatgpt.py

# ============================================================================
# EXECUTION MODE - LOCAL OU DISTANT (SSH)
# ============================================================================
# Choix du mode d'exécution : "local" ou "remote"
EXECUTION_MODE=local

# ============================================================================
# SSH CONFIGURATION (mode remote uniquement)
# ============================================================================
# Adresse IP ou hostname de la machine distante
SSH_HOST=192.168.1.100

# Nom d'utilisateur SSH
SSH_USER=admin

# Port SSH (optionnel, défaut: 22)
SSH_PORT=22

# Méthode 1 : Authentification par clé privée (recommandé)
# Chemin vers votre clé privée SSH
SSH_KEY_PATH=/home/user/.ssh/id_rsa
# ou sur Windows : C:\Users\username\.ssh\id_rsa

# Méthode 2 : Authentification par mot de passe (moins sécurisé)
# ⚠️ Non recommandé en production, utilisez plutôt des clés SSH
# SSH_PASSWORD=votre_mot_de_passe

# Note: Si SSH_KEY_PATH est défini, SSH_PASSWORD sera ignoré
# Si aucun des deux n'est défini, l'agent SSH système sera utilisé

# ============================================================================
# INSTRUCTIONS
# ============================================================================
# 1. Copiez ce fichier et renommez-le en ".env"
# 2. Remplacez les valeurs par vos vraies clés API
# 3. Ne committez JAMAIS le fichier .env dans Git (déjà dans .gitignore)
# 4. Installez python-dotenv : pip install python-dotenv
# 5. Ajoutez au début de main.py :
#    from dotenv import load_dotenv
#    load_dotenv()

# ============================================================================
# EXEMPLES D'UTILISATION
# ============================================================================

# Exemple 1 : Utiliser Claude Sonnet (recommandé)
# AI_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-api03-abc123...
# CLAUDE_MODEL=claude-sonnet-4-20250514

# Exemple 2 : Utiliser Claude Opus (meilleure qualité, plus cher)
# AI_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-api03-abc123...
# CLAUDE_MODEL=claude-opus-4-20250514

# Exemple 3 : Utiliser ChatGPT (plus rapide, moins cher)
# AI_PROVIDER=chatgpt
# OPENAI_API_KEY=sk-abc123...

# Exemple 4 : Exécution sur machine distante via SSH (avec clé privée)
# EXECUTION_MODE=remote
# SSH_HOST=192.168.1.100
# SSH_USER=admin
# SSH_PORT=22
# SSH_KEY_PATH=/home/user/.ssh/id_rsa

# Exemple 5 : Exécution sur machine distante via SSH (avec mot de passe)
# EXECUTION_MODE=remote
# SSH_HOST=192.168.1.100
# SSH_USER=admin
# SSH_PASSWORD=monmotdepasse

# ============================================================================
# SÉCURITÉ
# ============================================================================
# ⚠️ NE PARTAGEZ JAMAIS VOS CLÉS API
# ⚠️ NE COMMITTEZ JAMAIS LE FICHIER .env
# ⚠️ RÉVOQUEZ IMMÉDIATEMENT LES CLÉS COMPROMISES
