# ============================================================
# ShellIA - Environment Variables Template
# ============================================================
# Optional: copy to .env to override docker-compose defaults.
#   cp .env.docker .env
#
# Most settings can be edited directly in docker-compose.yml.
# ============================================================

# ── Exposed port ─────────────────────────────────────────────
SHELLIA_PORT=8000

# ── Timezone ─────────────────────────────────────────────────
TZ=Europe/Paris

# ── Execution environment ────────────────────────────────────
SHELLIA_ENV=local

# ── JWT Security ─────────────────────────────────────────────
# ⚠️  Generate a strong random key:
#     openssl rand -hex 32
SECRET_KEY=

# ── Google OAuth ──────────────────────────────────────────────
# Configured directly in docker-compose.yml (environment section).
# Do not put GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET here.
# Edit docker-compose.yml and fill in the values directly.
