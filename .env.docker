# ============================================================
# ShellIA - Variables d'environnement Docker
# ============================================================
# Copiez ce fichier en .env et renseignez vos valeurs
# Ne committez jamais ce fichier !
# ============================================================

# ── Port exposé ──────────────────────────────────────────────
SHELLIA_PORT=8000

# ── Fuseau horaire ───────────────────────────────────────────
TZ=Europe/Paris

# ── Environnement de démarrage ───────────────────────────────
SHELLIA_ENV=local

# ── Sécurité JWT ─────────────────────────────────────────────
# ⚠️  Générez une clé aléatoire forte :
#     openssl rand -hex 32
SECRET_KEY=change-me-in-production-please

# ── IA (optionnel - configurable aussi dans l'interface) ─────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
AI_PROVIDER=claude
CLAUDE_MODEL=claude-sonnet-4-20250514
